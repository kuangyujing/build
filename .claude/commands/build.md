Run the build script for the package: $ARGUMENTS

1. Find the matching script in `/Users/k/build/scripts/` (e.g. "vim" matches `build-vim.sh`)
2. Execute it and stream the output
3. If the build fails, read `config.log` or the error output and diagnose the issue
4. Report the result: success or failure with explanation
